<div class="section-header">
  <h2 class="h1 section-header__title section-header__left">{{ settings.home_feature_title }}</h2>
  <hr class="hr--small">
</div>
<div class="nav-bar desktop_nav third_product_disposition" id="container">
  

 {% for product in collections[settings.home_products].products %}
  
  {% comment %}
  Check if the product is on sale and set a variable to be used below.
{% endcomment %}
{% assign on_sale = false %}
{% if product.compare_at_price > product.price %}
  {% assign on_sale = true %}
{% endif %}

{% comment %}
  Check if the product is sold out and set a variable to be used below.
{% endcomment %}
{% assign sold_out = true %}
{% if product.available %}
  {% assign sold_out = false %}
{% endif %}
  
	{% assign var_count = forloop.index %}
  
  
  
  {% if var_count == 1 or var_count == 4 or var_count == 7 or var_count == 10 or var_count == 13 or var_count == 16 or var_count == 19 or var_count == 22 or var_count == 25 or var_count == 28 or var_count == 31 or var_count == 34 or var_count == 37 or var_count == 40 or var_count == 43 or var_count == 46 or var_count == 49 or var_count == 52 or var_count == 55 %}
<div class="grid__item collage-grid__row">
  
  {% endif %}
 
  
  
   {% if var_count == 1 or var_count == 5 or var_count == 7 or var_count == 11 or var_count == 13 or var_count == 17 or var_count == 19 or var_count == 23 or var_count == 25 or var_count == 29 or var_count == 31 or var_count == 35 or var_count == 37 or var_count == 41 or var_count == 43 or var_count == 47 or var_count == 49 or var_count == 53 or var_count == 55 %}
<div alt="{{ var_count }}" class="item large--two-thirds">
  {% assign image_size = "1024x1024" %}
 {% else %}
    {% assign image_size = "large" %}
  <div alt="{{ var_count }}" class="item large--one-third">
  
  {% endif %}
  
   
  
          
            {% comment %}
      You can show a leading 'from' or 'up to' by checking 'product.price_varies'
      if your variants have different prices.
    {% endcomment %}
   


    
   
        {% if sold_out %}
            <p class=" {{ sale_box_opt  }} hidesale"> 
               <strong>{{ settings.productsoldout }}</strong>
            </p>
       {% else %}
    
          <p class=" {{ sale_box_opt  }} hidesale">  
      {% if product.price_varies %}
          <strong>{{ settings.productsale }}</strong>  
            {% else %}
        <strong>{{ settings.productsale }}</strong>
     
      {% endif %}
            </p>
        
      
      
       {% endif%} 
      
      
      <a href="{{ product.url }}">
      <div class="h_pod">
        
         
             {% if settings.enable_product_variant_image %}
      
        <img src="{% if product.first_available_variant.image %} {{ product.first_available_variant.image | img_url:  image_size }} {% else %} {{ product | img_url:  image_size }} {%endif%}" alt="{{ product.featured_image.alt | escape }}" class="image">

      {% else %}
          <img src="{{ product | img_url:  image_size }}" alt="{{ product.featured_image.alt | escape }}" class="image">
    
     {% endif %}
        
      </div>
        <div class="home_prod_info-act">
         <h5 class="nav-bar desktop_nav title">{{ product.title }}</h5>
          <h5 class="nav-bar mobile_nav title">
            {% if settings.on_mobile_title_set == "on_mobile_title_set_line_one" %}
             {% assign setHlimit=settings.home_page_text_limit %}
            {{ product.title | truncate:setHlimit }}
            {% else %}
             {{ product.title }}
            {% endif %}
          </h5>
          
          -
          <!-- product price setting for variant or normal -->
      {% include 'product_price_setting' %}  
        </div></a>
      
    </div>
	

  {% if var_count == 3 or var_count == 6 or var_count == 9 or var_count == 12 or var_count == 15 or var_count == 18 or var_count == 21 or var_count == 24 or var_count == 27 or var_count == 21 or var_count == 24 or var_count == 27 or var_count == 21 or var_count == 24 or var_count == 27  %}
  </div> 
 

  
  {% endif %}


    {% endfor %}
  
</div>
  
  
</div>
  
  
  
  
  
  <!-- for mobile style -->
  
  
  <div class="main_Product_grid nav-bar mobile_nav third_product_disposition third_product_disposition_mobile" id="container">
  

 {% for product in collections[settings.home_products].products %}
  
  {% comment %}
  Check if the product is on sale and set a variable to be used below.
{% endcomment %}
{% assign on_sale = false %}
{% if product.compare_at_price > product.price %}
  {% assign on_sale = true %}
{% endif %}

{% comment %}
  Check if the product is sold out and set a variable to be used below.
{% endcomment %}
{% assign sold_out = true %}
{% if product.available %}
  {% assign sold_out = false %}
{% endif %}
  
	{% assign var_count = forloop.index %}
  
  
  
  <div alt="" class="item large--one-quarter">
  
  

    
          {% if sold_out %}
            <p class=" {{ sale_box_opt  }} hidesale"> 
               <strong>{{ settings.productsoldout }}</strong>
            </p>
       {% else %}
    
          <p class=" {{ sale_box_opt  }} hidesale">  
      {% if product.price_varies %}
          <strong>{{ settings.productsale }}</strong>  
            {% else %}
        <strong>{{ settings.productsale }}</strong>
     
      {% endif %}
            </p>
        
      
      
       {% endif%} 

    
      
      <a href="{{ product.url }}">
      <div class="h_pod">
        <!-- price setting for variant price or normal product -->
         {% include "image_setting" %}        
      </div>
        <div class="home_prod_info-act">
           <h5 class="nav-bar desktop_nav title">{{ product.title }}</h5>
          <h5 class="nav-bar mobile_nav title">
            {% if settings.on_mobile_title_set == "on_mobile_title_set_line_one" %}
              {% assign setHlimit=settings.home_page_text_limit %}
            {{ product.title | truncate:setHlimit }}
            {% else %}
             {{ product.title }}
            {% endif %}
           
          </h5>
     
          
          -
          
          
   {% include 'product_price_setting' %}  
        </div></a>
      
    </div>
	
{% if var_count == 4 or var_count == 8 or var_count == 12 or var_count == 16 or var_count == 20 or var_count == 24  %}
    <div class="clear_both"></div>
    {% endif %}

    {% endfor %}
  
</div>